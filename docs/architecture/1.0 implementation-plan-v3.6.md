# Implementation Plan for `cockpit-slurm` Project — Version 3.6

## 1. Purpose

This document defines the implementation plan for `cockpit-slurm` Version 3.6.

Version 3.6 continues the backend and frontend work established in Version 3.5, but changes the transport architecture:

* `cockpit-slurm` is the application and persistent backend service.
* `cockpit-slurm-channel` is removed.
* The React frontend communicates with the backend through `cockpit.channel()`.
* Cockpit provides the transport path between the frontend and the backend Unix socket.
* The `cockpit-slurm` application protocol remains independent of the Cockpit protocol.
* The backend owns resource state, cache, synchronization, commands, subscriptions, authorization, and IPC.
* The frontend owns presentation, UI state, and frontend-facing protocol handling.
* The backend must remain usable and testable without Cockpit.

The implementation plan is subordinate to:

`docs/architecture/0.1 development-cockpit-slurm-_v3.6.md`

The v3.6 development architecture document is the normative architectural reference.

---

# 2. Version 3.5 → Version 3.6 Changes

## 2.1 Transport architecture change

Version 3.5 contained three conceptual implementation areas:

```text
React
  │
  │ cockpit.channel()
  ▼
cockpit-slurm-channel
  │
  │ Unix socket
  ▼
cockpit-slurm-bridge
  │
  ▼
Backend services
```

Version 3.6 removes the intermediate application channel process:

```text
React
  │
  │ cockpit.channel()
  ▼
Cockpit transport
  │
  │ Unix socket
  ▼
cockpit-slurm
  │
  ├── IPC Server
  ├── Dispatcher
  ├── Query Service
  ├── Command Service
  ├── Resource Service
  ├── Cache
  ├── Event Bus
  └── Subscription Manager
```

The backend application name is:

```text
cockpit-slurm
```

The terms `cockpit-slurm-bridge` and `cockpit-slurm-channel` must not be used as primary Version 3.6 application component names.

---

## 2.2 Removed Version 3.5 component

The following Version 3.5 component is removed:

```text
cockpit-slurm-channel
```

The following are therefore no longer part of the v3.6 target architecture:

* `cockpit-slurm-channel` executable
* channel-specific Go package
* channel executable manifest entry
* channel-specific Unix socket forwarding process
* channel-specific application transport layer

The existing v3.5 channel implementation may remain temporarily in Git history or be removed from the working tree, but it must not be required for the v3.6 runtime architecture.

---

## 2.3 Persistent backend

The backend remains a persistent system service:

```text
cockpit-slurm.service
        │
        ▼
cockpit-slurm
```

The backend is not started once per `cockpit.channel()` request.

It remains alive independently of frontend connections and maintains the authoritative Slurm state cache.

The service owns:

* Slurm synchronization
* canonical resources
* resource cache
* event bus
* subscriptions
* IPC server
* query processing
* command processing
* authorization
* audit information where required

---

# 3. Version 3.6 Implementation Strategy

Version 3.6 should be implemented as a sequence of vertical milestones rather than as independent frontend and backend projects.

The recommended sequence is:

```text
| Phase | Focus                              | Main question                                        |
| ----- | ---------------------------------- | ---------------------------------------------------- |
| 0     | Architecture/contracts             | Are the contracts stable?                            |
| 1     | Backend foundation                 | Does the backend work independently?                 |
| 2     | Frontend/backend integration       | Does Cockpit transport work?                         |
| 3     | Node read vertical slice           | Can React read a real resource?                      |
| **4** | **Node events/subscriptions**      | Can Node state stay synchronized?                    |
| **5** | **Node commands/authorization**    | Can Node be safely modified?                         |
| **6** | **Resource system generalization** | Can the proven mechanisms support other resources?   |
| 7     | UI integration                     | Can the frontend expose the resource system cleanly? |
| 8     | Production hardening               | Is the system production-ready?                      |
```

Both Phase 4 and Phase 5 explicitly Node-focused.

---

# 4. Phase 0 — Freeze Version 3.6 Contracts

## 4.1 Objective

Confirm that the contracts produced during Version 3.5 remain valid under the Version 3.6 transport architecture.

The following contracts must remain application-owned:

```text
IPC protocol
Resource model
UserContext
Event model
Command model
Error model
```

They must not depend on:

* Cockpit channel message boundaries
* React components
* PatternFly
* Cockpit bridge framing
* `cockpit-slurm-channel`

---

## 4.2 Application IPC framing

The application protocol remains length-prefixed.

Conceptually:

```text
┌──────────────┬──────────────────────┐
│ frame length │ application payload  │
└──────────────┴──────────────────────┘
```

The application frame may contain:

```json
{
  "protocol": "cockpit-slurm",
  "version": "1.0",
  "messageId": "01...",
  "type": "query",
  "resource": "nodes",
  "payload": {}
}
```

The backend must parse application frames independently of Cockpit.

The frontend integration layer must also reconstruct complete application frames from the arbitrary byte chunks delivered by `cockpit.channel()`.

---

## 4.3 UserContext

The server remains authoritative for user identity and authorization.

The backend derives identity from the server-side connection rather than trusting frontend-supplied identity.

The conceptual model remains:

```text
Cockpit authenticated user
        │
        ▼
Unix socket peer identity
        │
        ▼
UserContext
        │
        ├── Username
        ├── UID
        ├── GID
        ├── AdminLevel
        ├── Coordinator
        ├── Accounts
        └── Capabilities
```

The frontend may receive a safe representation of `UserContext`, but it must not establish the authoritative identity itself.

---

# 5. Phase 1 — Backend Foundation

## 5.1 Objective

Complete the persistent `cockpit-slurm` backend independently of React.

The backend must be able to run:

```text
systemd
   │
   ▼
cockpit-slurm
   │
   ▼
Unix socket
   │
   ▼
IPC Server
```

without requiring Cockpit.

---

## 5.2 Backend components

The implementation should retain the following conceptual components:

```text
cmd/cockpit-slurm/
    main.go

internal/
    ipc/
    protocol/
    dispatcher/
    context/
    capability/
    resource/
    cache/
    event/
    subscription/
    query/
    command/
    adapter/
```

The exact package layout may evolve during implementation, but architectural ownership must remain clear.

---

## 5.3 IPC server

The IPC server must:

* create/listen on the configured Unix socket;
* accept concurrent connections;
* read application frames;
* enforce frame-size limits;
* reject malformed frames;
* derive peer identity;
* construct the connection context;
* forward valid messages to the dispatcher;
* write application frames;
* close connections cleanly.

The IPC server must not contain Slurm-specific business logic.

---

## 5.4 Dispatcher

The dispatcher must:

* decode the application envelope;
* validate protocol version;
* validate message type;
* validate required fields;
* route requests;
* return protocol errors;
* preserve message IDs.

The dispatcher must remain independent of Cockpit.

---

## 5.5 Query path

The initial query path should be:

```text
IPC
  ↓
Dispatcher
  ↓
Query Controller
  ↓
Query Service
  ↓
Resource Cache
  ↓
Snapshot
  ↓
IPC
```

No Slurm command should be executed directly by the Query Controller.

---

## 5.6 Command path

The command path should be:

```text
IPC
  ↓
Dispatcher
  ↓
Command Controller
  ↓
UserContext
  ↓
Capability Check
  ↓
Command Service
  ↓
Command Adapter
  ↓
Slurm
```

Authorization remains a backend responsibility.

---

## 5.7 Phase 1 acceptance criteria

Phase 1 is complete when:

* `cockpit-slurm` builds;
* the Unix socket is created correctly;
* multiple connections are supported;
* peer identity can be established;
* application frames can be read and written;
* malformed frames are rejected;
* dispatcher tests pass;
* query and command interfaces have stable contracts;
* `go test ./...` passes;
* `go vet ./...` passes.

---

# 6. Phase 2 — Frontend/Backend Integration

## 6.1 Objective

Phase 2 is the first Version 3.6 integration milestone.

The purpose is to prove:

```text
React
  │
  │ cockpit.channel()
  ▼
Cockpit
  │
  │ stream + Unix socket
  ▼
cockpit-slurm
  │
  ▼
IPC Server
```

without:

```text
cockpit-slurm-channel
```
Phase 2 is implemented incrementally according to:

    `docs/architecture/4.1 Implementation-phase2.md`

This document defines the implementation sequence and acceptance
criteria for Phase 2. It is subordinate to the v3.6 development
architecture and implementation plan.

---

## 6.2 Important transport rule

Cockpit `stream` is a byte stream.

Application message boundaries must therefore not depend on:

```text
channel.onmessage
```

being one application message.

A single application frame may arrive as:

```text
chunk A
chunk B
chunk C
```

or multiple application frames may arrive in:

```text
one chunk
```

Therefore:

```text
Cockpit stream framing
        ≠
cockpit-slurm application framing
```

The application length-prefix decoder must remain in the frontend integration layer and backend IPC layer.

---

## 6.3 Frontend Cockpit channel

The intended frontend transport is conceptually:

```typescript
const channel = cockpit.channel({
    payload: "stream",
    unix: "/run/cockpit-slurm/cockpit-slurm.sock",
    binary: true,
});
```

The exact API behavior must be verified against the Cockpit version supported by the project before freezing the implementation.

The current Cockpit protocol specifies `unix` for the `stream` payload and describes `stream` as raw data whose boundaries are arbitrary.

---

## 6.4 Frontend integration layer

Create a small frontend transport layer, for example:

```text
src/ipc/
    channel.ts
    framing.ts
    client.ts
    types.ts
```

Responsibilities:

### `channel.ts`

Own:

* creation of `cockpit.channel()`;
* channel lifecycle;
* close/error handling;
* sending bytes;
* receiving bytes.

### `framing.ts`

Own:

* application frame encoding;
* application frame decoding;
* buffering incomplete frames;
* handling multiple frames in one chunk;
* frame-size validation.

### `client.ts`

Own:

* request/response correlation;
* message IDs;
* request lifecycle;
* application-level errors.

React components must not directly implement frame parsing.

---

## 6.5 First integration message

The first end-to-end test should be deliberately simple.

For example:

```text
React
  ↓
ping
  ↓
cockpit.channel()
  ↓
Unix socket
  ↓
cockpit-slurm IPC Server
  ↓
Dispatcher
  ↓
pong
  ↓
Unix socket
  ↓
cockpit.channel()
  ↓
React
```

The UI should display the returned message.

This is preferable to immediately implementing Slurm resources.

---

## 6.6 Integration test

The first integration page should demonstrate:

```text
Connection status
Message sent
Message received
Message ID
Round-trip latency
Connection error
Connection closed
```

The page is a development/test page and does not need to be the final Slurm UI.

---

## 6.7 Socket installation modes

The v3.6 architecture should define one authoritative production socket:

```text
/run/cockpit-slurm/cockpit-slurm.sock
```

A user-local socket may be supported for development, but it should be treated as a development installation mode rather than an alternative runtime identity architecture.

Do not select the production socket based on whether the current user belongs to `wheel`.

The backend should instead use:

* socket ownership;
* socket group;
* socket permissions;
* system security policy;
* server-side peer credentials.

The backend derives the user identity from the accepted Unix socket connection.

---

## 6.8 Phase 2 acceptance criteria

Phase 2 is complete when:

1. React can create a Cockpit channel.
2. Cockpit can connect the channel to the configured Unix socket.
3. The backend accepts the connection.
4. The backend identifies the peer.
5. React can send an application frame.
6. The backend can decode it.
7. The backend can return an application frame.
8. React can reconstruct the frame.
9. Message IDs are preserved.
10. Partial frame delivery works.
11. Multiple frames in one stream chunk work.
12. Channel close is handled correctly.
13. Backend disconnect is reported to React.
14. Multiple concurrent frontend connections work.
15. `cockpit-slurm-channel` is not required.

---

# 7. Phase 3 — First Vertical Slice

## 7.1 Objective

After transport integration succeeds, implement the first real Slurm resource.

Recommended first resource:

```text
Node
```

The complete path should be:

```text
Slurm
  ↓
Node Resource Adapter
  ↓
Canonical Node
  ↓
Resource Cache
  ↓
Query Service
  ↓
IPC
  ↓
Cockpit channel
  ↓
Frontend IPC client
  ↓
Node Provider
  ↓
Node Table
```

This proves that the application is not merely exchanging test messages.

---

## 7.2 Snapshot request

Example:

```json
{
  "protocol": "cockpit-slurm",
  "version": "1.0",
  "messageId": "01...",
  "type": "query",
  "resource": "nodes",
  "payload": {}
}
```

Response:

```json
{
  "protocol": "cockpit-slurm",
  "version": "1.0",
  "messageId": "01...",
  "type": "snapshot",
  "resource": "nodes",
  "generation": 1,
  "payload": {}
}
```

---

## 7.3 Phase 3 acceptance criteria

The first vertical slice is complete when:

* a real Slurm resource is read;
* the resource is normalized;
* the resource is stored in the cache;
* React can request a snapshot;
* React displays the resource;
* no Slurm command is executed by React;
* no Cockpit-specific logic exists in backend business services.

---

# 8. Phase 4 — Node Events and Subscriptions

Not delay all generic Resource System work after phase 5 in Phase 6.

There should be a small amount of generic infrastructure in Phase 3/4.

For example, when implementing Node, you should already define interfaces roughly along these lines conceptually:
```text
Resource
ResourceAdapter
ResourceStore
ResourceSnapshot
ResourceEvent
```
But don't attempt to implement every possible generic feature.

Think:

>generic contracts early, generic implementation after experience.

This gives you both advantages.
Node Events
```text
Node
 ↓
Cache
 ↓
Change detection
 ↓
Event
 ↓
Subscription
 ↓
Frontend
```

Events
```text
NodeUpdated
NodeStateChanged
```
Authorization

You can test:

```text
ordinary user
    ↓
cannot modify node

operator/admin
    ↓
may modify node
```

So Node can exercise:
```text
Query
Cache
Generation
Event
Subscription
Command
Authorization
Audit
Frontend state
```

Implement:

```text
Cache update
     ↓
Event Bus
     ↓
Subscription Manager
     ↓
IPC
     ↓
React
```

Subscription lifecycle:

```text
subscribe
    ↓
initial snapshot
    ↓
events
    ↓
unsubscribe
```

Subscriptions must be resource-oriented rather than UI-component-oriented.

Good:

```json
{
  "type": "subscribe",
  "resource": "node"
}
```

Avoid:

```json
{
  "type": "subscribe",
  "component": "NodeTable"
}
```

The backend knows resources, not React components.

---

# 9. Phase 5 — Node Commands and Authorization

Implement command execution after read/query and event synchronization are stable. Make it specifically Node first.


```text
React
 ↓
Command
 ↓
Authorization
 ↓
Command Service
 ↓
Node Command Adapter
 ↓
Slurm
 ↓
Node cache refresh
 ↓
Node event
 ↓
React
```
This is particularly important because it validates the **full mutation lifecycle.**

don't want:
```text
command succeeded
      ↓
frontend guesses new state
```
want:
```text
command
  ↓
Slurm changes
  ↓
resource synchronization
  ↓
cache update
  ↓
event
  ↓
frontend
```
That is a much stronger architecture.

Command flow:

```text
React
  ↓
Application Command
  ↓
IPC
  ↓
Command Controller
  ↓
UserContext
  ↓
Capability Resolver
  ↓
Authorization
  ↓
Command Service
  ↓
Command Adapter
  ↓
Slurm
```

The backend must always enforce authorization.

Frontend visibility is only a user-experience mechanism.

## 9.1 Command tracking

Commands should have:

* command ID;
* request message ID;
* command type;
* requesting user;
* lifecycle state;
* result;
* error;
* audit information where required.

command adapter using commands: 
https://slurm.schedmd.com/scontrol.html#SECTION_COMMANDS
```shell
scontrol update Nodename=... 
scontrol power
scontrol reboot
scontrol create
scontrol show 
scontrol shutdown
...
```

---

# 10. Phase 6 — Resource System Generalization

Objective: Generalize the proven Node query, cache, event, subscription, command, and authorization mechanisms into reusable resource infrastructure, then implement additional resource types.(Generalize the resource infrastructure from the completed Node implementation, then add additional resource types.)

Phase 6 establishes the reusable Resource System by generalizing the Node implementation completed in Phase 3,4,5.

The phase must demonstrate that additional Slurm resources can be introduced through resource-specific adapters and canonical models without duplicating the IPC, Query Service, Cache, generation,snapshot, or frontend transport infrastructure.


Frontend services and providers must not become a second implementation of the backend Resource System.

Phase 3,4,5 proved:
```text
One resource:
Node
```

The implementation order could be:

```text
Node (Phase 3,4,5)
 │
 ├── Query       ✓
 ├── Cache       ✓
 ├── Events      ✓
 ├── Subscribe   ✓
 ├── Commands    ✓
 └── Auth        ✓
        │
        ▼
  Extract generic mechanisms (this phase)
        │
        ├──────────────┐
        ▼              ▼
       Job            User
        │              │
        ▼              ▼
     Cluster         Account
```

This is a much more convincing architectural milestone.


Phase 6 should prove:
```text
Reusable resource architecture:
Node + another resource
```

## This sequence gives you a nice separation:
```text
Backend
────────────────────────────

Resource-specific:
    NodeAdapter
    JobAdapter
    UserAdapter

Generic:
    ResourceService
    ResourceCache
    EventBus
    SubscriptionManager
    CommandService


Frontend
────────────────────────────

Generic:
    IPC Client
    Resource Service
    Command Service
    Subscription Service

Resource-specific:
    NodeProvider
    JobProvider
    UserProvider
```
And importantly:

>Frontend services remain transport/client abstractions.

They do not become another resource framework.

That is exactly the boundary you recently added to the architecture document.

## Implement:

```text
Resource Adapter
      ↓
Resource Service
      ↓
Canonical Resource
      ↓
Resource Cache
```

Good example:

```text
NodeProvider
    ↓
resourceService.query("nodes")
    ↓
IPC
    ↓
Query Service
    ↓
Resource Cache
```

The resource system should support:

* resource identity;
* generation;
* timestamps;
* canonical models;
* indexing;
* lookup;
* snapshots;
* refresh;
* change detection.

The cache is the authoritative in-memory state of the backend.

* frontend services = transport/client abstractions.
* providers own frontend/client resource state.
* backend Resource Cache is authoritative.
* Phase 6 as Resource System Generalization.

After Node is complete, you should generalize things like:
```text
ResourceIdentity
ResourceGeneration
ResourceCache
Snapshot
ChangeDetection
Event
Subscription
QueryRequest
QueryResponse
CommandRequest
CommandResponse
```
But do not try to make all Slurm resources look identical.

For example:
```text
Node
 ├── state
 ├── CPUs
 ├── memory
 ├── partitions
 └── features

Job
 ├── job ID
 ├── user
 ├── state
 ├── partition
 ├── resources
 └── time

User
 ├── UID
 ├── account
 ├── administrator
 └── coordinator

Account
 ├── name
 ├── organization
 ├── description
 └── associations
```
These should remain **resource-specific canonical models.**

What becomes generic is the infrastructure surrounding them.

After Node is fully implemented:
```text
                 Generic infrastructure
                         │
        ┌────────────────┼────────────────┐
        │                │                │
        ▼                ▼                ▼
      Node              Job             User
        │                │                │
     Adapter          Adapter          Adapter
        │                │                │
   Canonical Node   Canonical Job   Canonical User
        │                │                │
        └────────────────┼────────────────┘
                         ▼
                  Resource Service
                         │
                  Resource Cache
                         │
              Event / Subscription
                         │
                   Query Service
                         │
                  Command Service
```
The **generic layer is extracted from working Node behavior.**

That is much safer than designing a universal resource abstraction up front.


---
# 11. Phase 7 — React Architecture

Only after the first vertical slice works should the frontend architecture be generalized.

Recommended structure:

```text
Backend:
    authoritative Slurm/application state
        ↓
    Resource Cache

Frontend:
    client-side resource presentation state
        ↓
    NodeProvider / JobProvider / AccountProvider

UI:
    component-local presentation state
        ↓
    tables/forms/dialogs/etc.
```

Providers should own frontend resource state and frontend-facing
communication for the resources they expose.

Providers are not authoritative application-state owners.

The authoritative Slurm/application state remains in the backend
Resource Cache.

Frontend services are transport/client abstractions, not
business-logic services.

The frontend must not:
  * execute Slurm commands;
  * implement Slurm business rules;
  * implement backend authorization;
  * maintain an authoritative resource cache;
  * duplicate backend synchronization logic.

Frontend providers may:
  * request resource snapshots;
  * subscribe to resource updates;
  * maintain client-side resource state;
  * expose loading/error/connection state;
  * coordinate data required by React components.

```text
Backend
┌────────────────────────────────────┐
│ Canonical Node                     │
│ Resource Cache                     │
│ Event / Subscription               │
│ Query Service                      │
└────────────────────────────────────┘
                 │
                 │ IPC
                 ▼
Frontend
┌────────────────────────────────────┐
│ IPC Client                         │
│ Resource Services                  │
│ NodeProvider                       │
│   - Node snapshot                  │
│   - Node event state               │
│   - loading/error state            │
└────────────────────────────────────┘
                 │
                 ▼
          NodeTable / UI

```
Do not create a separate `cockpit.channel()` for every React component.

A provider should normally share the connection and state among the components that consume the same resource.

---

# 12. Phase 8 — Role-Based UI

After the backend `UserContext` is available end-to-end, implement role-based UI.

Conceptual roles:

```text
Visitor / Not Set
User
Coordinator
Operator
Admin
```

The preferred decision path is:

```text
Backend UserContext
        ↓
Capability Resolver
        ↓
Frontend UserContext
        ↓
Navigation Policy
        ↓
Home Page Policy
        ↓
UI
```

The frontend should prefer capability checks such as:

```text
nodes.view
nodes.modify
jobs.view
jobs.cancel
accounts.view
accounts.modify
```

rather than duplicating backend authorization rules.

---

# 13. Phase 9 — Production Hardening

After the functional vertical slices are complete, implement:

* reconnect;
* connection timeout;
* request timeout;
* backpressure;
* frame-size limits;
* event recovery;
* stale generation detection;
* command idempotency;
* audit logging;
* structured logging;
* metrics;
* socket security;
* graceful service shutdown;
* frontend error recovery;
* backend restart recovery.

---

# 14. Version 3.5 Work Reused by Version 3.6

Version 3.6 should not restart the backend foundation created during Version 3.5.

The following work remains valuable:

| v3.5 work                   | v3.6 treatment       |
| --------------------------- | -------------------- |
| IPC server                  | Retain and integrate |
| Protocol framing            | Retain               |
| Protocol envelope           | Retain               |
| Dispatcher                  | Retain               |
| Connection model            | Retain               |
| UserContext                 | Retain and complete  |
| Capability resolver         | Retain and extend    |
| Error model                 | Retain               |
| Resource contracts          | Retain               |
| Command model               | Retain               |
| Event model                 | Retain               |
| Subscription model          | Retain               |
| `cockpit-slurm-channel`     | Remove               |
| Channel-specific forwarding | Remove               |
| Channel manifest entry      | Remove               |
| Direct Cockpit transport    | New in v3.6          |
| Frontend IPC client         | New in v3.6          |
| End-to-end integration      | New in v3.6          |

---

# 15. Version 3.6 Milestones

```text
M0  Architecture / Contracts
 │
 ▼
M1  Backend Foundation
 │
 ▼
M2  Frontend / Backend Integration
 │
 ▼
M3  First Vertical Slice — Node Query
 │
 ▼
M4  Node Events & Subscriptions
 │
 ▼
M5  Node Commands & Authorization
 │
 ▼
M6  Resource System Generalization
 │
 ├── Node remains reference implementation
 ├── Job
 ├── Cluster
 ├── User
 ├── Account
 └── other resources
 │
 ▼
M7  UI Integration
 │
 ▼
M8  Production Hardening
```


## M0 — Architecture Frozen

Deliver:

```text
v3.6 architecture document
IPC protocol
Resource model
UserContext
Command model
Event model
Error model
```

Status:

```text
Target
```

---

## M1 — Backend Foundation

Deliver:

```text
cockpit-slurm
    ↓
Unix socket
    ↓
IPC Server
    ↓
Dispatcher
```

Acceptance:

```text
go test ./...
go vet ./...
```

---

## M2 — Frontend/Backend Integration

Deliver:

```text
React
    ↓
cockpit.channel()
    ↓
Cockpit
    ↓
Unix socket
    ↓
cockpit-slurm
```

No:

```text
cockpit-slurm-channel
```

Acceptance:

```text
ping → pong
```

plus stream-framing and connection lifecycle tests.

---

## M3 — First Vertical Slice - Node

Recommended:

```text
Node
```

Deliver:

```text
Slurm
 ↓
Node Adapter
 ↓
Cache
 ↓
Query
 ↓
IPC
 ↓
React
```

---
## M4 — Node Resource Events and Subscriptions

Deliver:

```text
Slurm
 ↓
Resource Refresh
 ↓
Cache
 ↓
Event Bus
 ↓
Subscription
 ↓
React
```

---

## M5 — Node Commands and Authorization

Deliver initial commands such as:

```text
cancel
resume
suspend
```

with:

```text
Command ID
Authorization
Command status
Audit
```

---

## M6 — Resource System Generalization

Deliver Node Resource System Generalization

---

## M7 — User/Account + Role-Based UI

Deliver:

```text
User
Account
Association
Coordinator
AdminLevel
UserContext
```

Deliver:

```text
User
Coordinator
Operator
Admin
```

with capability-driven navigation and pages.

---

## M8 — Production Hardening

Deliver:

```text
Reconnect
Timeouts
Backpressure
Event recovery
Generation validation
Audit
Logging
Metrics
Security
```

---

# 16. Recommended Implementation Order

The recommended Version 3.6 implementation order is:

```text
                    ┌─────────────────────┐
                    │  v3.6 Architecture  │
                    └──────────┬──────────┘
                               │
                               ▼
                    ┌─────────────────────┐
                    │ Backend Foundation  │
                    │ IPC + Dispatcher    │
                    └──────────┬──────────┘
                               │
                               ▼
              ┌────────────────────────────────┐
              │ Frontend / Backend Integration │
              │ cockpit.channel() + Unix socket│
              └───────────────┬────────────────┘
                              │
                              ▼
                    ┌─────────────────────┐
                    │ First Vertical Slice │
                    │       Node           │
                    └──────────┬──────────┘
                               │
              ┌────────────────┴────────────────┐
              ▼                                 ▼
     ┌─────────────────┐               ┌──────────────────┐
     │ Resource System │               │ Events/Subscribe │
     └────────┬────────┘               └────────┬─────────┘
              └────────────────┬────────────────┘
                               ▼
                    ┌─────────────────────┐
                    │ Commands + Auth     │
                    └──────────┬──────────┘
                               │
                               ▼
                    ┌─────────────────────┐
                    │ React Architecture  │
                    │ Providers + UI       │
                    └──────────┬──────────┘
                               │
                               ▼
                    ┌─────────────────────┐
                    │ Production Hardening│
                    └─────────────────────┘
```

---

# 17. Relationship to `4.2 Implementation-phase3.md`
The document:

```text
cockpit-slurm Implementation Phase 3 — First Vertical Slice: Nodes 
```

is an implementation document for **Phase 3 / M3** of this plan.

It should not become a competing architecture document.

The Version 3.6 architecture document remains authoritative for architectural decisions.

The Version 3.6 implementation plan remains authoritative for implementation sequencing.

The `4.2` document remains an implementation guide for the frontend/backend integration milestone.

---

# 18. Definition of Version 3.6 Completion

Version 3.6 is considered functionally complete when:

```text
React
  │
  │ cockpit.channel()
  ▼
Cockpit transport
  │
  │ Unix socket
  ▼
cockpit-slurm
  │
  ├── UserContext
  ├── Dispatcher
  ├── Query Service
  ├── Command Service
  ├── Resource Service
  ├── Resource Cache
  ├── Event Bus
  └── Subscription Manager
  │
  ▼
Slurm
```

works end-to-end with:

* multiple concurrent users;
* server-derived identity;
* canonical resources;
* resource snapshots;
* resource events;
* commands;
* server-side authorization;
* frontend providers;
* role-based UI;
* reconnect/error handling;
* no `cockpit-slurm-channel`.

The backend remains independently testable and independently executable without Cockpit.

---

# 19. Version 3.6 Architectural Rule

The central rule for Version 3.6 is:

> **Cockpit transports application bytes; `cockpit-slurm` owns application semantics.**

Therefore:

```text
Cockpit
    = transport

cockpit.channel()
    = frontend transport API

Unix socket
    = transport endpoint

cockpit-slurm IPC protocol
    = application protocol

cockpit-slurm
    = application/backend

React
    = presentation and frontend state
```

No component should blur these boundaries.
